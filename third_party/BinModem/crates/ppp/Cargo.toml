[package]
name    = "ppp"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# For the frame check sequence. RFC 1662's FCS-16 and V.42's are the same CRC
# from the same HDLC lineage -- the same generator, the same reflection, the
# same residue over a good frame -- so there is one of it rather than two.
ec = { path = "../ec" }

[lints]
workspace = true
