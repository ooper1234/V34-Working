# V.34 / V.90 / V.92 — implementation status and engineering report

Authority: ITU-T V.34, V.90, V.92. Reference implementation in the tree:
spanDSP `v34tx.c`, `v34rx.c`, `v34_logging.c`, `v34_tables.h`,
`v34_superconstellation_map.h` (LGPL 2.1), extended by
`third_party/spandsp/src/spandsp-v34-phase3.patch` (this project).

## Executive summary

The spanDSP V.34 engine has been made to build and run in this repository and
has been extended so that a full loopback call now completes:

```
V.8 -> V.34 phase 1 -> phase 2 (INFO0, A/B tones, INFO1, L1/L2 probing)
    -> phase 3 (S/!S, PP, TRN, J, J')
    -> phase 4 (second TRN, MP/MPH exchange, MP', E)
```

All of these transitions are driven by decoded signals (coherent signal
detection and CRC-checked messages), not by timers or energy heuristics.

What is verified:

1. **Mapping engine (bit-exact).** `v34_mapping` round-trips data bits
   through two modem contexts (S/Q/I parsing, superframe structure, shell
   mapping, differential encoding, trellis/precoding, scrambling and the
   inverse demapper) with **0 mismatches at 4800, 9600, 19200 bps (2400 baud),
   16800 (2743), 21600 (2800), 24000 (3000), 28800 (3200) and 33600 bps
   (3429)**.

2. **Full start-up and control exchange (loopback).** `v34_loopback` shows
   V.8 negotiation, phase-1 INFO0 exchange, A/B tone sequencing, phase-2
   INFO1c/INFO1a exchange with correct parameters, L1/L2 line-probe DFT, the
   S/!S alignment, PP/TRN training, J/J', the post-J TRN, the MP/MPH exchange
   ("Rx MP:", "far MP received, sending MP'", "MP' sent twice, sending E",
   "e_baud_init") on both sides.

The transmit side is also complete: after the E sequence both modems enter
the primary-channel data mode (`Tx - data_baud_init`, stage DATA) and send
data symbols from the verified mapping engine.

On the receive side, the E detection, receive-mode switch
(`Rx - E seen, starting primary channel`), per-baud symbol collection and the
demapper (`v34_put_mapping_frame`) are wired up, and bits are produced. The
receiver front end is not complete, though: symbol timing acquisition, AGC,
the data-level calibration and the equalizer are not implemented, so the
demapped bits are not valid data yet (about 50% errors in loopback, which is
what the `v34_loopback` test reports). The unit is:
`process_primary_half_baud()` in `v34rx.c` plus `primary_channel_rx()` (which
had a hardcoded baud rate/carrier bug that has been fixed).

## What was implemented in this tree

- `build_v34.sh` generates every table with spanDSP's own generators (RRC
  sets, shell map, trellis coders, probe signals, pre-emphasis with the
  vendored meteor engine) and builds `libsmv34.a`; leftover per-sample debug
  printfs are suppressed by `spandsp-v34-debug-printfs.patch`.
- `spandsp-v34-phase3.patch` adds, over upstream:
  - **Phase-3 S detection** (`v34rx.c`): two coherent correlators, at the
    far end's V.34 carrier and at this side's control-channel carrier. On the
    real waveform the S signal presents as coherent V.34-carrier energy
    (normalised ratio ~0.3) with no control-channel tone; the phase-1 A/B
    tones present as a 0.707 ratio tone on the control-channel carrier and
    the PP/TRN training is incoherent. Three qualifying 10 ms blocks latch
    `V34_EVENT_S`.
  - **Transmit sequencing** (`v34tx.c`): the caller waits on the S event
    (staying on its baud generator so the event is re-checked), both sides
    transmit S/!S/PP/TRN, the caller sends J', the answerer leaves J for the
    post-J TRN, and the post-J TRN switches the receiver to the
    control-channel demodulator.
  - **MP/MPH completion**: sent on the control channel; when the far end's
    CRC-validated MP has been decoded it is acknowledged with MP', sent
    twice, then the E sequence begins.
  - New state fields in `spandsp/private/v34.h` (`s_signal_seen` and its
    correlator state, `trn_after_j`, `mp_tx_count`).

## Test suite (all real, reproduce below)

| Test | Result |
|---|---|
| `v22bis_loopback 2400` | 0 errors both directions (regression) |
| `v22bis_pty 2400 5 256` | 0 mismatches both directions (regression) |
| `v22bis_cross --rate 2400` | PASS (spanDSP interop, regression) |
| `v34_mapping` rate matrix | 0 mismatches, 4800-33600 bps |
| `v34_loopback 2400 4800 30` | V.8 -> phase 4 MP/MPH/E reached on both sides |

## Exact remaining blockers

1. **Data/constellation amplitude calibration** between the training
   sequence and the primary-channel data (the `35.77` factor in an upstream
   comment is the only hint; the V.34 text defines the actual transmit
   levels). The data generator currently emits one constellation unit as
   1.0, matching the Q9.7 mapping format.
2. **Primary-channel receiver front end.**
   Fixed this session:
   - `primary_channel_rx` hardcoded baud rate 5 / low carrier; now uses the
     negotiated values.
   - `steps_per_baud[]` held a whole-baud increment while
     `process_primary_half_baud()`, the Godard TED and the equalizer all
     expect one insertion per half baud (the working CC receiver uses a T/2
     increment). The table now holds T/2 increments (320 at 2400 baud),
     matching the working structure.
   - The primary TED coefficients were created from the `high_carrier` flag
     and the baud rate *code* (carrier 1 Hz, 0 baud) instead of the real
     carrier frequency and symbol rate; now correct.
   - The E sequence is transmitted as 20 ones through the scrambler, which is
     what the receiver's descrambled-bits check requires.
   Still open: with all of the above the demodulated data symbols are not on
   a clean constellation grid, so the demapper sees corrupted symbols and
   produces about 50% bit errors. The following were tested and did NOT fix
   it (all real loopback measurements):
   - primary Godard TED enabled and disabled (disabling makes the symbols
     larger and more regular, evidence that the TED mis-steers timing, but the
     bit error rate is unchanged);
   - receive scale sweeps 0.5, 1.0, 1.2, 1.5, 2.0, 4.0, 10.0;
   - initial fractional sampling phase 0, 32, 64, 96, 128, 160;
   - mapping-frame alignment offsets 0-7;
   - I/Q conjugation of the received symbols.
   The V.34 RX filter tables were verified to be correct: computed with the
   right DFT sign convention they are a flat 1.0 bandpass from 1000 to
   2600 Hz rolling off at 600/3000 Hz, exactly the 2400-baud RRC at an
   1800 Hz carrier, using the same generation convention as the working
   V.22bis/CC tables.
   `tools/v34_frontend.c` is an offline replica of `primary_channel_rx`
   (same tables, same fractional-timing loop) that reproduces the daemon's
   values from a waveform capture; the next step is to use it to compare the
   recovered symbols against a known transmitted frame position by position,
   which will show whether the corruption is in the receive filtering/timing
   or in the transmitted waveform itself.
   Diagnostic hooks (environment variables, no effect on normal runs):
   V34_RX_OFFSET, V34_RX_SCALE, V34_RX_INITPHASE, V34_RX_CONJ, V34_RX_ROTDEG.

   Progress from the deterministic ideal-path work
   (`tools/v34_ideal_path.c`):
   - The test generates a known symbol sequence with the transmit mapping
     engine, synthesizes the exact data-mode waveform (same polyphase RRC
     and carrier as tx_v34_modulation) and decodes it with the real receiver
     front end and demapper in a fresh state. Result: 4800 bits, 0
     mismatches, and also 0 mismatches when the waveform is round-tripped
     through G.711 mu-law. This proves the receiver filtering, timing grid,
     demapper and the data generator are all functionally correct.
   - Two transmit-side bugs were found and fixed with it:
     * tx_v34_modulation read its 9-element pulse-shaping buffer linearly
       (`[i + step]`, up to index 16), i.e. out of bounds. The working
       control-channel modulator uses a circular dot product; the data path
       now indexes circularly too.
     * The control-channel modulator leaves baud_phase in 0..39 while the
       primary modulator's polyphase table has only `num` (10) phases and
       indexes it with num-1-baud_phase. Entering data mode with an
       inherited phase indexed the shaper out of bounds. The reset now
       happens at the first primary-modulator call (deferred so the final
       E symbol is still sent correctly).
   - The loopback harness now uses per-direction PRBS data (constant data is
     pathological for this scrambler: all-ones is its fixed point, so the
     line signal stays constant and nothing decodes).
   - Remaining: with all of the above, the live loopback still shows ~49%
     BER while the ideal path decodes bit-exactly. The live capture's
     spectrum differs from a synthesis generated from the live transmitter's
     own logged symbols, so the search continues in the live transmit path
     or in the handover state at the E switch. V34_SYM_TRACE=1 logs the
     transmit symbols for that comparison.
   Older notes:
   - `pri_symbol_sync()` must be verified to acquire and hold the symbol
     timing from the start of data mode (the current samples vary in
     magnitude by more than an order of magnitude at 1600+ symbols in).
   - AGC and the receive-level calibration from signal units to the Q9.7
     values the demapper expects (`data_rx_scale`, currently 1.0).
   - Carrier phase tracking must hold the constellation in the slicing grid
     (the demapper's slicer operates before the differential decoder).
   - The equalizer (`s->eq_buf`/`eq_coeff`, currently unused in this path).
3. **Level calibration** of the data constellation against the training
   sequence (the `35.77` factor in an upstream comment is the only hint; the
   V.34 text defines the actual transmit levels).
4. **V.90/V.92**: no engine exists anywhere; they sit above a working V.34
   data mode. The PCM transport (G.711/RTP in `sm_sip`) is ready.

## How to continue

1. Add `get_data_baud()` and the `E -> data` transition in `v34tx.c`,
   using `v34_get_mapping_frame()` per 8-baud frame.
2. Build the primary-channel receiver around the existing timing, equalizer
   and Viterbi pieces, then verify with `v34_loopback` (ideal channel) and
   add a channel model.
3. Only after data bits flow end to end, enable V.34 in the daemon's V.8
   capabilities (currently only V.22 is advertised, which keeps the working
   hardware path untouched).
